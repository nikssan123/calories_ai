/**
 * The cast's geometry lives in `@ct/shared/cast`, so the web can draw the same
 * figures as the phone and the widget. Re-exported here so everything in the
 * app keeps importing it from beside the components that use it.
 */
export * from '@ct/shared/cast';
